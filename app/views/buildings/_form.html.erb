<%= form_with(model: building) do |form| %>
  <% if building.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(building.errors.count, "error") %> prohibited this building from being saved:</h2>

      <ul>
        <% building.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_for @building do |f| %>

    <div class="field">
      <%= f.label :物件名 %>
      <%= f.text_field :building_name %>
    </div>

    <div class="field">
      <%= f.label :賃料 %>
      <%= f.number_field :price %>
    </div>

    <div class="field">
      <%= f.label :住所 %>
      <%= f.text_field :address %>
    </div>

    <div class="field">
      <%= f.label :築年数 %>
      <%= f.number_field :building_age %>
    </div>

    <div class="field">
      <%= f.label :備考 %>
      <%= f.text_area :info %>
    </div>

    <Hr>

    <%= f.fields_for :station_infos do |station_info| %>

      <h2>最寄り駅1</h2>

        <div class="field">
          <%= station_info.label :路線名 %>
          <%= station_info.text_area :station_route %>
        </div>

      <h2>最寄り駅2</h2>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
