<%= form_for @building do |f| %>
  <% if @building.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(building.errors.count, "error") %> prohibited this building from being saved:</h2>

      <ul>
        <% building.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :物件名 %>
      <%= f.text_field :building_name %>
    </div>

    <div class="field">
      <%= f.label :賃料 %>
      <%= f.number_field :price %>円
    </div>

    <div class="field">
      <%= f.label :住所 %>
      <%= f.text_field :address %>
    </div>

    <div class="field">
      <%= f.label :築年数 %>
      <%= f.number_field :building_age %>年
    </div>

    <div class="field">
      <%= f.label :備考 %>
      <%= f.text_area :info %>
    </div>

    <Hr>

    <%= f.fields_for :station_infos,@building.station_infos.build do |station_info| %>

      <h3>最寄り駅1</h3>
      <div class="field">
        <%= station_info.label :路線名 %>
        <%= station_info.text_field :station_route %>
      </div>

      <div class="field">
        <%= station_info.label :駅名 %>
        <%= station_info.text_field :closest_station_name %>
      </div>

      <div class="field">
        <%= station_info.label :徒歩分数 %>
        <%= station_info.text_field :walk_time %>分
      </div>

      <h3>最寄り駅2</h3>
      <div class="field">
        <%= station_info.label :路線名 %>
        <%= station_info.text_field :station_route %>
      </div>

      <div class="field">
        <%= station_info.label :駅名 %>
        <%= station_info.text_field :closest_station_name %>
      </div>

      <div class="field">
        <%= station_info.label :徒歩分数 %>
        <%= station_info.text_field :walk_time %>分
      </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
<% end %>
